import { NavItem } from '@/types';
import {
    Package,
    Users,
    LayoutGrid,
    CreditCard,
} from 'lucide-react';

export const icons = {
    Package,
    Users,
    LayoutGrid,
    CreditCard,
};